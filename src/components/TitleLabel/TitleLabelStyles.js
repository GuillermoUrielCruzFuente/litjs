import { css } from "lit-element";

export const titleLabelStyles = css`
	* {
		margin: 0;
		padding: 0;
		line-height: 1;
	}

	.topic {
		font-size: 0.75rem;
		text-transform: uppercase;
		font-weight: 900;
		color: #999;
	}

	.text {
		color: #ddd;
	}
`;
